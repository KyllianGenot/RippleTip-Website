# Discord Integration (Client-Side)
# Used for Discord OAuth login on the frontend.
# Get these from your Discord Developer Portal application.
VITE_DISCORD_CLIENT_ID="<your_discord_client_id>"
VITE_DISCORD_REDIRECT_URI="<your_discord_redirect_uri>" # e.g., http://localhost:3000/callback

# MoonPay Integration (Client-Side)
# API Key for interacting with MoonPay services.
VITE_MOONPAY_API_KEY="<your_moonpay_api_key>"

# Backend API Configuration (Client-Side)
# The base URL for your backend API server.
VITE_API_BASE_URL="<your_backend_api_base_url>" # e.g., http://localhost:3001/api

# Server-Side Secrets (If applicable)
# These might be needed if you have a server-side component or build process
# that reads environment variables directly without the VITE_ prefix.
# If these are purely server-side and not exposed to the client,
# they might belong in a different .env file for that server process.
CLIENT_SECRET="<your_client_secret>" # Often used with OAuth or API authentication
SESSION_SECRET="<your_session_secret>" # Used for securing user sessions